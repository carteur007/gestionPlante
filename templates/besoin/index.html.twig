{% extends 'base.html.twig' %}

{% block title %}Besoin index{% endblock %}

{% block body %}
    <h1>Besoin index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>QuantiteEau</th>
                <th>FrecArrosage</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for besoin in besoins %}
            <tr>
                <td>{{ besoin.id }}</td>
                <td>{{ besoin.quantiteEau }}</td>
                <td>{{ besoin.frecArrosage }}</td>
                <td>
                    <a href="{{ path('app_besoin_show', {'id': besoin.id}) }}">show</a>
                    <a href="{{ path('app_besoin_edit', {'id': besoin.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="4">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_besoin_new') }}">Create new</a>
{% endblock %}
