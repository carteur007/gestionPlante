{{ form_start(form) }}
{{ form_widget(form) }}
<div class="center-align" style="margin-top:1vw;">
	<button type="submit" class="btn-flat btn-large orange">{{ button_label|default('Enregistrer') }}</button>
</div>
{{ form_end(form) }}
